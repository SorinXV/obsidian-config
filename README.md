## 🛠️ Installed Community Plugins

Here is the list of community plugins configured and used in this vault:

*   **Obsidian Git** (`obsidian-git`) — Backup and sync the vault using Git.
*   **Style Settings** (`obsidian-style-settings`) — Controls and customizes theme options.
*   **Icon Folder** (`obsidian-icon-folder`) — Adds custom icons to folders and files.
*   **Advanced Tables** (`table-editor-obsidian`) — Improved markdown table editing and formatting.
*   **Automatic Table of Contents** (`automatic-table-of-contents`) — Dynamically generates a TOC for notes.
*   **Better Export PDF** (`better-export-pdf`) — Enhances markdown-to-PDF rendering and layouts.
*   **Calendar** (`calendar`) — Simple calendar view for tracking daily notes.
*   **Code Styler** (`code-styler`) — Customizes the appearance of code blocks and inline code.
*   **Desmos** (`obsidian-desmos`) — Integrates Desmos graphing calculators into notes.
*   **Editing Toolbar** (`editing-toolbar`) — Adds a visual, MS Word-style text formatting toolbar.
*   **Canvas Export** (`canvas-export`) — Allows exporting Obsidian Canvas views to standard image formats.
*   **Excalidraw** (`obsidian-excalidraw-plugin`) — Advanced sketching and infinite canvas drawing tool.
*   **File Explorer Plus** (`file-explorer-plus`) — Enhances the native file explorer side panel functionality.
*   **Highlightr** (`highlightr-plugin`) — Adds rich, colorful text highlighting options.
*   **Obsidian Importer** (`obsidian-importer`) — Helps migrate notes from Evernote, Notion, Keep, and more.
*   **Lazy Plugins** (`lazy-plugins`) — Delays plugin loading on startup to optimize vault launch speeds.
*   **Mathpad** (`mathpad`) — Real-time mathematical evaluations and scratchpad.
*   **Omnisearch** (`omnisearch`) — Intelligent, deep-search tool that indexes text inside PDFs and images.
*   **Peerdraft** (`peerdraft`) — Enables real-time, peer-to-peer collaborative note editing.
*   **Pretty Properties** (`pretty-properties`) — Improves the visual style and layout of frontmatter/YAML tags.
*   **Solve** (`solve`) — Inline calculator that computes equations as you type them.
*   **Templater** (`templater-obsidian`) — Powerful templating language to automate note creation.
*   **Various Complements** (`various-complements`) — Rich auto-complete dictionary for faster typing.
*   **Webpage HTML Export** (`webpage-html-export`) — Converts markdown documents into pixel-perfect HTML files.
*   **PDF Plus** (`pdf-plus`) — Drastically upgrades the native PDF viewing, highlighting, and linking experience.

---
